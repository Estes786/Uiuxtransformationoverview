
  # UI/UX Transformation Overview

  This is a code bundle for UI/UX Transformation Overview. The original project is available at https://www.figma.com/design/y8cdkg7d9xNhQfpxQCCmmO/UI-UX-Transformation-Overview.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  